x; align-self: stretch; border-radius: 999px; background: var(--accent); }
  .question-text { font-size: 14px; line-height: 1.28; color: var(--primary); }
  .question-text strong { font-weight: 800; }
  .content-grid {
    display: grid; grid-template-columns: .76fr 1.74fr; gap: 10px; align-items: stretch; flex: 1; min-height: 0;
  }
  .card, .graph-panel { background: var(--surface); border: 1px solid var(--line); border-radius: 24px; box-shadow: var(--shadow-card); }
  .side-card { padding: 10px; display: flex; flex-direction: column; gap: 8px; min-width: 0; }
  .side-head {
    display: flex; align-items: center; justify-content: space-between; gap: 10px;
    background: linear-gradient(180deg,#d9ecfb 0%,#eef6fd 100%);
    padding: 10px 12px; border-radius: 14px;
  }
  .side-title { font-size: 20px; font-weight: 800; color: var(--primary); }
  .side-sub { font-size: 11px; letter-spacing: .02em; color: var(--primary-2); text-transform: none; margin-top: 4px; }
  .side-icon {
    width: 40px; height: 40px; display: grid; place-items: center; border-radius: 12px;
    background: rgba(255,255,255,.34); color: var(--primary); font-size: 20px; font-weight: 800;
  }
  .equation-box {
    min-height: 92px; border-radius: 16px; padding: 12px;
    background: linear-gradient(180deg,#f9fbff 0%,#f3f7fc 100%);
    border: 1px solid rgba(214,226,240,.65);
    display: flex; align-items: center; justify-content: center; text-align: center;
  }
  .equation-text { font-family: var(--font-math); font-size: clamp(18px,1.6vw,24px); color: var(--primary); }
  .mini-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 6px; }
  .mini-grid.single { grid-template-columns: 1fr; }
  .mini-card {
    border-radius: 12px; padding: 7px 8px 6px 8px; background: linear-gradient(180deg,#f8fbff 0%,#f3f8fd 100%);
    border: 1px solid rgba(214,226,240,.85);
  }
  .mini-label { font-size: 10px; text-transform: none; letter-spacing: .01em; color: var(--text-faint); font-weight: 700; }
  .mini-value { margin-top: 3px; font-size: 14px; color: var(--primary); font-weight: 800; font-family: var(--font-body); }
  .choice-row { display: flex; gap: 6px; flex-wrap: wrap; }
  button.small-btn {
    appearance: none; border: 1px solid rgba(35,166,213,.25); background: #eef6fd; color: var(--primary);
    border-radius: 10px; padding: 7px 10px; font-size: 12px; font-weight: 800; cursor: pointer;
    transition: transform var(--transition-fast), background var(--transition-fast), color var(--transition-fast), border-color var(--transition-fast);
  }
  button.small-btn.active, button.small-btn:hover {
    background: linear-gradient(90deg,var(--primary),var(--accent)); color: #fff; border-color: transparent; transform: translateY(-1px);
  }
  .tagline { font-size: 12px; color: var(--text-soft); line-height: 1.34; }
  .graph-panel { padding: 8px; display: flex; align-items: center; justify-content: center; min-height: 220px; overflow: hidden; }
  .viz-holder { width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; }
  .viz-holder svg { width: 100%; height: 100%; display: block; }
  .controls {
    position: fixed; left: 10px; right: 10px; bottom: 8px; z-index: 20;
    display: flex; align-items: center; justify-content: space-between; gap: 16px;
  }
  .controls-right { display: flex; align-items: center; gap: 8px; }
  button.nav-btn {
    appearance: none; border: none; cursor: pointer; padding: 8px 12px; border-radius: 999px; font-size: 13px; font-weight: 700;
    transition: transform var(--transition-fast), opacity var(--transition-fast);
  }
  button.nav-btn:hover { transform: translateY(-1px); }
  button.nav-btn:disabled { opacity: .42; cursor: not-allowed; transform: none; }
  .btn-secondary { background: rgba(22,58,99,.10); color: var(--primary); }
  .btn-primary { color: #fff; background: linear-gradient(90deg,var(--primary),var(--primary-2)); }
  .ghost-signature { position: fixed; right: 12px; bottom: 5px; font-size: 9px; colo